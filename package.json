{
  "name": "pearl-dex",
  "version": "0.0.1",
  "description": "Trustless, non-custodial peer-to-peer exchange between Pearl (PRL) and Bitcoin (BTC). Off-chain order matching, on-chain atomic-swap settlement, forfeitable bonds to neutralize the free-option problem.",
  "type": "module",
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^2.2.0",
    "boltz-core": "^5.0.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
